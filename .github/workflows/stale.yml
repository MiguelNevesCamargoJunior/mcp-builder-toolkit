# Stale bot is disabled during alpha.
# Re-enable when there is consistent community triage volume.
#
# To enable: set `schedule` and add `actions/stale` step.
# See https://github.com/actions/stale for configuration.

name: Stale (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Stale bot is disabled during alpha"
