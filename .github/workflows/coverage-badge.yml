name: Coverage badge (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coverage badge is updated manually. Run scripts/update_coverage_badge.py"

# The badge was previously auto-committed, but branch protection rules
# require all changes to go through PRs. Automated badge updates created
# commit churn and CI trigger loops without adding meaningful value.
