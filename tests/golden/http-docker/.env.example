# Copy to .env and adjust. Never commit real secrets.
# Generated by MCP Builder Toolkit — no credentials are embedded.

# Streamable HTTP bind settings (override as needed)
MCP_HOST=127.0.0.1
MCP_PORT=8000
